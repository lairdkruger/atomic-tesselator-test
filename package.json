{
  "name": "atomic-tesselator-test",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm run --parallel -r dev",
    "build": "pnpm run --parallel -r build",
    "preview": "pnpm run --parallel -r preview",
    "lint": "pnpm run --parallel -r lint"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
